'use strict';

angular.
  module('loginform').
	component('loginForm', {
		
		templateUrl:'loginform/loginform.template.html',
		controllerAs: 'login',
		controller:['$http',function loginformController($http) {
		
	
		
		}] 
	
	});